
int main() {
    int a, b, c, d, e, f, result;
    // 为变量赋初值
    a = 5; b = 10; c = 15; d = 20; e = 25; f = 30;
    // 布尔表达式，覆盖所有运算符
    // 包含：|| && == != > < >= <= ! 括号 常量 标识符
    result = !(a > b) && (c == d) || (e != f) && (a >= b) || (c < d) && !(e <= f);
    return 0;
}