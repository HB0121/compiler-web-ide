# 题目一代码说明：词法分析器的关键实现

本文档不再按实验报告的写法展开，而是直接围绕 [task1_lexer.py](/D:/02_Development_Projects/CompilationprinciplesCourse/lab1/%E9%A2%98%E7%9B%AE%E4%B8%80/task1_lexer.py) 中最关键的实现逻辑说明：程序如何扫描输入、如何决定进入哪类识别分支，以及为什么这些分支顺序不能随便改。

## 1. 先看整体：这份代码到底在做什么

`task1_lexer.py` 的目标不是“把文件读出来”，而是把源程序拆成一串有类型的 token。

每个 token 记录三件事：

- `lexeme`：原始单词内容
- `token_type`：种别码
- `line`：所在行号

这意味着程序的核心任务其实只有一句话：

```text
从左到右扫描字符，在正确的时机切出一个完整单词，并给它正确的种别码。
```

真正承担这个任务的，不是 `main()`，也不是 `Token`，而是 `Lexer.tokenize()`。

## 2. 为什么 `tokenize()` 是全程序核心

`tokenize()` 本质上是一个“总调度器”。

它维护四个关键状态：

- `self.source`：完整源码
- `self.index`：当前扫描位置
- `self.line`：当前行号
- `self.tokens`：最终输出结果

主循环很简单：

```python
def tokenize(self) -> List[Token]:
    while self.index < self.length:
        ch = self.source[self.index]
```

但这段循环的重要性很高，因为它决定了两件事：

- 当前字符应该交给哪个识别函数处理
- 当前识别完成后，`index` 和 `line` 应该推进到哪里

可以把它理解成状态机的入口：每轮先读当前字符，再决定“跳到哪个识别状态”。

## 3. 最关键的设计：按“首字符类型”分流

这份词法分析器没有把所有逻辑堆进一个大函数里，而是采用了“主循环分流 + 专用扫描函数处理细节”的结构。

`tokenize()` 中最关键的一组判断是：

```python
if ch.isalpha() or ch == "_":
    self._scan_identifier_or_keyword()
    continue

if ch.isdigit():
    self._scan_number()
    continue

if ch == "'":
    self._scan_char_literal()
    continue

if ch == '"':
    self._scan_string_literal()
    continue
```

这里体现的是一个经典词法分析思路：

- 先看“这个单词可能从什么字符开始”
- 再把后续细节交给专门函数完成

这样做有两个好处：

- 主流程清楚，读者很容易看出整个扫描器的工作顺序
- 每一类 token 的规则被封装在独立函数里，后续扩展更方便

## 4. 为什么判断顺序不能乱

这份代码里最值得讲的，不是“有哪些 if”，而是“为什么这些 if 的顺序是现在这样”。

### 4.1 注释判断必须早于 `/` 运算符

代码先判断：

```python
if ch == "/" and self._peek() == "/":
    self._skip_single_line_comment()
    continue

if ch == "/" and self._peek() == "*":
    self._skip_multi_line_comment()
    continue
```

然后才进入普通运算符识别。

原因很直接：如果先把 `/` 当成普通运算符处理，那么：

- `// comment` 会被错误拆成两个 `/`
- `/* comment */` 会被错误拆成 `/` 和 `*`

所以注释识别必须优先。

### 4.2 双字符运算符必须早于单字符运算符

代码里专门有：

```python
if self._scan_double_operator():
    continue
```

并且它出现在单字符运算符前面。

这是为了避免把：

- `<=` 拆成 `<` 和 `=`
- `>=` 拆成 `>` 和 `=`
- `==` 拆成 `=` 和 `=`

也就是说，这里不是简单的“多写一个函数”，而是在解决最长匹配问题：

```text
如果当前位置既能组成一个更长的合法 token，就优先识别更长的那个。
```

这就是词法分析里非常关键的实现习惯。

## 5. `_scan_identifier_or_keyword()` 的核心价值

这个函数不是单纯“读一个字符串”，它实际上同时解决了两件事：

1. 先把连续的字母/数字/下划线整体读完
2. 再判断这个 lexeme 到底是关键字还是普通标识符

核心逻辑是：

```python
while self.index < self.length and (
    self.source[self.index].isalnum() or self.source[self.index] == "_"
):
    self.index += 1

lexeme = self.source[start:self.index]
token_type = KEYWORDS.get(lexeme, IDENTIFIER)
```

这里最关键的点在第二步：

```text
关键字和标识符的扫描规则几乎一样，区别不在“怎么读”，而在“读完后怎么判定类型”。
```

例如：

- `int` 的扫描过程和 `main` 完全一样
- 真正的区别只发生在最后查 `KEYWORDS` 那一步

这是一个很值得在文档里强调的实现思想。

## 6. `_scan_number()` 真正处理的不是“数字”，而是分叉

这个函数最重要的地方不在“循环读数字”，而在它处理了整数和浮点数的分叉。

逻辑可以概括为：

1. 先吃掉整数部分
2. 如果后面没有 `.`，那就是整数
3. 如果后面有 `.`，再看下一个字符是不是数字
4. 如果是，则继续读成浮点数
5. 如果不是，则把前面的部分作为整数，把 `.` 当成独立运算符

这段设计很关键，因为它对应了你这份代码的一个明确选择：

```text
在题目一里，20. 不会报错，而是被拆成整数 20 和运算符 .
```

这和题目二的错误处理策略正好形成对比，所以这里值得在文档里单独说明。

## 7. 字符串和字符常量：为什么要单独扫描

`_scan_char_literal()` 和 `_scan_string_literal()` 的共同点是：

- 都不能只看一个字符
- 都必须考虑转义字符
- 都需要自己决定“什么时候才算结束”

例如字符常量：

- `'a'`
- `'\n'`

字符串常量：

- `"hello"`
- `"a\tb"`

它们和标识符、整数最大的不同在于：

```text
它们的边界不是靠“遇到非法字符就停”，而是靠“遇到匹配的结束引号才结束”。
```

因此必须使用独立函数去维护内部扫描规则。

## 8. `_peek()` 的作用不是辅助，而是“前瞻”

`_peek()` 看起来很小，但它支撑了两个关键实现：

- 注释识别
- 浮点数判断

例如：

- 当前字符是 `/`，只有知道下一个是不是 `/` 或 `*`，才能判断是不是注释
- 当前字符序列是 `20.`，只有继续看下一个字符是不是数字，才能判断这是浮点数还是“整数 + 点号”

也就是说，`_peek()` 解决的是：

```text
当前字符本身不够决定 token 类型，必须再看后面一个字符。
```

这是词法分析器里非常典型的“前瞻一位”实现。

## 9. 这份代码里最容易被忽略的两个细节

### 9.1 行号维护不是附属功能

代码里多处更新 `self.line`：

- 在 `tokenize()` 中遇到换行
- 在 `_scan_string_literal()` 中遇到换行
- 在 `_skip_multi_line_comment()` 中跨行时

这说明行号维护不是最后补上去的，而是整个扫描过程的一部分。否则最终输出的第三列就会不准确。

### 9.2 `Token.__str__()` 直接决定输出是否能过评测

```python
return "{:<16}{:<8}{}".format(self.lexeme, self.token_type, self.line)
```

这个格式化函数不是“展示层小细节”，而是评测输出格式的一部分。对这种实验题来说，识别正确和输出格式正确同样重要。

## 10. 推荐的读代码顺序

如果要真正看懂这份实现，建议按下面顺序：

1. 先看 `main()`，知道程序入口和输入来源。
2. 再看 `tokenize()`，理解总调度流程。
3. 然后看 `_scan_identifier_or_keyword()` 和 `_scan_number()`，这是最核心的两个识别函数。
4. 最后看字符、字符串、注释和双字符运算符处理。

## 11. 一句话总结这份实现

这份词法分析器最关键的实现思想不是“把各种 token 都列出来”，而是：

```text
由 tokenize() 负责统一调度，由各个 _scan_* 函数负责局部识别，并通过前瞻、最长匹配和行号维护保证结果正确。
```

HuangBin