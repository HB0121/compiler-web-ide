# 编译器 Web IDE 手动测试指南

> 作者：黄彬 (12303070250)
> 测试日期：2026-05-13

---

## 一、环境准备

### 1.1 启动后端（Spring Boot）

```bash
cd compiler-backend
mvn spring-boot:run
```

后端启动成功标志：控制台显示 `Started CompilerBackendApplication`，监听端口 `8080`。

### 1.2 启动前端（Vue + Vite）

```bash
cd compiler-frontend/compiler-frontend
npm run dev
```

浏览器打开 `http://localhost:5173`，应看到 Monaco 编辑器（左侧）和结果面板（右侧）。

### 1.3 使用 curl 测试（可选）

如果你习惯命令行，可以在后端启动后直接用 curl 发 POST 请求：

```bash
curl -X POST http://localhost:8080/api/compile \
  -H "Content-Type: application/json" \
  -d '{"sourceCode": "你的C代码"}'
```

---

## 二、功能模块测试

### 2.1 词法分析（Tokens 标签页）

**测试代码：**
```c
void main(){
    int x;
    x = 3 + 5 * 2;
    write(x);
}
```

**操作：** 粘贴代码到编辑器 → 点击 ▶ 运行 → 切换到 `Tokens` 标签页

**预期结果：**
- 显示 token 列表，每行包含 Line / Col / Text / Type / Code
- 应包含 22 个 token（含 `void`, `main`, `(`, `)`, `{`, `int`, `x`, `;`, `=`, `3`, `+`, `5`, `*`, `2`, `write`, 等）
- 关键字类型为 `keyword`，数字为 `int_literal`，标识符为 `identifier`，标号为 `operator`

**验证：** `x` 的 kind 应为 `identifier`，`3` 的 kind 应为 `int_literal`，`write` 的 kind 应为 `keyword`

---

### 2.2 语法分析（AST 标签页）

**测试代码：** 同上

**操作：** 切换到 `AST` 标签页

**预期结果：**
- 以缩进树形展示抽象语法树
- 根节点为 `Program`
- 子节点包含 `FuncDecl → Compound → VarDecl / Assign / FuncCall`
- 表达式的 BinOp 节点正确嵌套（`+` 下有 `Literal(3)` 和 `BinOp(*)`）

**验证：** 展开后能清晰看到 `x = (3 + (5 * 2))` 的树形结构

---

### 2.3 中间代码生成（IR 标签页）

**测试代码：** 同上

**操作：** 切换到 `IR`（四元式）标签页

**预期结果：**
- 显示四元式序列，每条格式为 `(op, arg1, arg2, result)`
- 应包含类似 `(*, 5, 2, t1)`, `(+, 3, t1, t2)`, `(=, t2, _, x)` 的四元式
- 最后两条应为 `(CALL, main, 0, _)` 和 `(EXIT, _, _, _)`

**验证：** 四元式顺序符合运算优先级（先乘除后加减）

---

### 2.4 解释执行（Output 标签页）

**测试代码：** 同上

**操作：** 切换到 `Output`（运行结果）标签页

**预期结果：**
```
🚀 开始解释执行 (已启用调用栈机制)...
🖨️: 13
✅ 执行成功！结束时全局变量状态: {}
```

**验证：** 输出 `13`（即 3 + 5 × 2 = 13）

---

### 2.5 MASM 汇编代码生成（MASM 标签页）

**测试代码：** 同上

**操作：** 切换到 `MASM` 标签页

**预期结果：**
- 显示完整的 MASM 16-bit 汇编代码
- 包含 `.MODEL SMALL`, `.STACK 100h`, `.DATA`, `.CODE` 段
- 包含 `_write_int` 和 `_read_int` 辅助子程序
- `_entry:` 为程序入口点
- 数据段包含变量定义（`_v_x DW 0` 等）

**复制功能：** 点击右上角 `📋 复制汇编代码` 按钮，粘贴到文本编辑器中验证完整内容

---

### 2.6 LLVM IR 代码生成（LLVM 标签页）★ 选做 4.2

**测试代码：** 同上

**操作：** 切换到 `LLVM` 标签页

**预期结果：**
- 显示标准 LLVM IR 代码
- 以 `declare i32 @write_int(i32)` 开始
- `define i32 @main()` 包含函数体定义
- 变量有 `alloca` 声明，计算前有 `load`，结果用 `store` 写回
- 算术运算用 `add`, `mul`, `sdiv` 等 LLVM 指令

**验证命令（可选）：**
```bash
# 复制 LLVM IR 到 test.ll，用 lli 直接运行
lli test.ll
```

**复制功能：** 点击右上角 `📋 复制 LLVM IR` 按钮

---

### 2.7 控制流图 + DAG 优化（CFG 标签页）★ 选做 4.4

**测试代码（触发公共子表达式消除）：**
```c
void main(){
    int a; int b; int c; int d;
    a = 3 + 4;
    b = 3 + 4;    // 与上一行重复计算
    c = a * a;
    d = a * a;    // 与上一行重复计算
    write(c);
    write(d);
}
```

**操作：** 切换到 `CFG`（流图/DAG）标签页

**预期结果：**

**上半部分 — 控制流图：**
- 显示基本块列表（B0, B1, ...）
- 每个块标注四元式范围、前驱块、后继块
- 底部列出流图边（B0 → B2 等）

**下半部分 — DAG 局部优化：**
- 显示 DAG 节点创建过程
- `(+, 3, 4, t1)` → n0[t1]
- `(+, 3, 4, t2)` ⚡ 公共子表达式消除: 复用 n0
- 显示统计：`共消除公共子表达式: 4 条`

**验证：** `eliminatedCount` 应为 4（3+4 重复 1 次 + = 重复 2 次 + a*a 重复 1 次）

---

### 2.8 编译流水线日志（Log 标签页）

**测试代码：** 任意有效代码

**操作：** 切换到 `Log` 标签页

**预期结果：**
- 以表格形式展示 7~8 个编译阶段的耗时和输入/输出
- 底部汇总：`总阶段: 7 | 总耗时: Xms`

### 2.8.1 正则→NFA→DFA 日志扫描 ★ 选做 4.1

**说明：** 日志扫描器独立于编译器流水线运行，不需要有效的 C 代码。在 Log 标签页输入日志文本和正则表达式后点击 ▶ 运行即可。

**操作步骤：**
1. 切换到 `Log` 标签页
2. 在「日志文本」文本框粘贴待扫描的日志内容（每行一条记录）
3. 在「正则表达式」输入框输入正则（支持 `|`、`*`、`()`、`\` 转义）
4. 编辑器中的 C 代码可以留空或使用 `void main(){}`
5. 点击 ▶ 运行
6. 在 Log 标签页下半部分查看 NFA / DFA / 扫描结果

**curl 直接测试：**
```bash
curl -X POST http://localhost:8080/api/compile \
  -H "Content-Type: application/json" \
  -d '{
    "sourceCode": "void main(){}",
    "logText": "ERROR disk full\nWARN memory low\nINFO start",
    "regex": "ERROR|WARN"
  }'
```
返回的 `logScanner` 字段包含 `ast`、`nfa`、`dfa`、`scanResults`。

---

**测试 1：基本并运算 `ERROR|WARN`**

日志文本：
```
2024-01-15 ERROR disk full
2024-01-15 INFO server started
2024-01-15 WARN memory low
2024-01-16 ERROR network timeout
```
正则表达式：`ERROR|WARN`

预期结果：
| 检查项 | 预期值 |
|---|---|
| 正则 AST | `(((((ER)R)O)R)\|(((WA)R)N))` |
| NFA 状态数 | 20 |
| ε 转移边 | 10 条（连接各字符的 NFA 片段） |
| DFA 状态数 | 10 |
| DFA 接受状态 | 2 个（状态 8 和 状态 9） |
| 扫描匹配行 | 3 行（ERROR/WARN/ERROR），INFO 行不匹配 |
| 匹配位置 | `[11-15] ERROR`，`[11-14] WARN` |

---

**测试 2：Kleene 闭包 `a*b`**

日志文本：
```
b
ab
aab
aaab
```
正则表达式：`a*b`

预期结果：
| 检查项 | 预期值 |
|---|---|
| NFA 状态数 | 6 |
| DFA 状态数 | 4 |
| 行 1 "b" 匹配 | `[0-0] b` |
| 行 2 "ab" 匹配 | `[0-1] ab` |
| 行 4 "aaab" 匹配 | `[0-3] aaab` |
| 扫描匹配行 | 全部 4 行 |

**测试 3：分组闭包 `(ab)*`**

日志文本：
```
ab
abab
aba
```
正则表达式：`(ab)*`

预期结果：
| 检查项 | 预期值 |
|---|---|
| NFA 状态数 | 6 |
| 行 1 "ab" 匹配 | `[0-1] ab`（匹配一个 ab） |
| 行 2 "abab" 匹配 | `[0-3] abab`（匹配两个 ab） |
| 行 3 "aba" 匹配 | `[0-1] ab`（第3个字符 a 无法匹配 b，停在 ab） |

**测试 4（可选）：转义字符**

正则表达式：`a\|b`（匹配字面量 "a|b" 这个字符串）

日志文本：`a|b`
预期：匹配 `[0-2] a|b`（三个字符都匹配）

---

**原理说明：**

```
用户输入            正则解析器           Thompson 构造法
"ERROR|WARN"  ──→  AST(Union/Concat)  ──→  NFA(状态+ε转移)

    子集构造法                       DFA 扫描
NFA ──→ DFA(状态转移表)  ──→  逐行匹配日志文本 → 匹配结果
```

- **Thompson 构造法**：每个字符生成 2 状态 NFA，再通过 ε 转移组合成并/连接/闭包
- **子集构造法**：计算 NFA 的 ε-闭包，将状态集映射为 DFA 状态，消除非确定性
- **DFA 扫描**：从起始状态出发，按字符转移，到达接受状态时记录最长匹配

---

### 2.9 诊断错误（Err 标签页）

**测试代码（含语法错误）：**
```c
void main(){
    int x;
    x = 5 + ;    // 语法错误：缺少操作数
}
```

**操作：** 运行后切换到 `Err` 标签页

**预期结果：**
- 编译失败，右上角显示 ❌ 编译失败
- 诊断列表显示 parser 错误，描述 token 缺失

---

## 三、综合测试用例

### 3.1 函数调用 + 参数传递

**输入：**
```c
int add(int a, int b){
    return a + b;
}
void main(){
    int x;
    x = add(5, 8);
    write(x);
}
```

**预期输出：** `13` （5 + 8 = 13）
**验证模块：** Interpreter, MASM (CALL/RET 指令), LLVM (参数 %p → call)

---

### 3.2 条件分支 if/else

**输入：**
```c
void main(){
    int x; int y;
    x = 10; y = 0;
    if(x > 5){
        y = 1;
    } else {
        y = 2;
    }
    write(y);
}
```

**预期输出：** `1` （10 > 5 为真，执行 then 分支）
**验证模块：** IR (J!≠ 跳转), MASM (cmp/jne 指令), LLVM (icmp/br)

---

### 3.3 while 循环

**输入：**
```c
void main(){
    int i; int s;
    i = 1; s = 0;
    while(i <= 5){
        s = s + i;
        i = i + 1;
    }
    write(s);
}
```

**预期输出：** `15` （1 + 2 + 3 + 4 + 5）
**验证模块：** IR (回填/backpatch), MASM (循环跳转), Interpreter (循环执行)

---

### 3.4 read() 内置函数

**输入：**
```c
void main(){
    int x;
    x = read();
    write(x * x);
}
```

**预期输出：** `81` （模拟输入 9，执行输出 9 × 9 = 81）
**验证模块：** Interpreter (READ 模拟), MASM (_read_int 子程序)

---

### 3.5 多函数递归

**输入：**
```c
int fib(int n){
    if(n <= 1){
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}
void main(){
    write(fib(6));
}
```

**预期输出：** `8` （斐波那契数列 1, 1, 2, 3, 5, 8 → fib(6) = 8）
**验证模块：** Interpreter (递归调用栈), IR (PARAM/CALL/RET 序列)

---

## 四、故障排查

| 问题 | 可能原因 | 解决方法 |
|---|---|---|
| 前端空白/加载失败 | 后端未启动或端口冲突 | 检查 `localhost:8080/api/compile` 是否可访问 |
| Monaco 编辑器不显示 | 网络问题 | 确保能访问 cdn.jsdelivr.net |
| 编译报 CORS 错误 | 跨域未配置 | 确认 CompileController 有 @CrossOrigin |
| 四元式/汇编为空 | 语法不完整 | 检查 Err 标签页中的诊断信息 |
| LLVM IR 或 CFG 为空 | 前端未刷新 | 确认后端为最新编译版本 |
| 正则扫描无结果 | logText 或 regex 为空 | 两个输入框都必须填写内容 |
| NFA/DFA 不显示 | 正则语法错误 | 检查正则表达式括号是否匹配，`*` 前是否有操作数 |
| 扫描遗漏匹配行 | 正则写法问题 | 用更简单的正则（如单个字符）验证 DFA 正确性 |

---

## 五、测试完成检查清单

- [ ] 词法分析：Tokens 表显示 20+ token
- [ ] 语法分析：AST 树正确展示程序结构
- [ ] 中间代码：四元式序列包含算术/跳转/函数调用
- [ ] 解释执行：Output 显示正确的计算结果
- [ ] MASM 代码：汇编代码完整，可复制
- [ ] LLVM IR：标准 LLVM IR 格式，变量有 load/store ★
- [ ] 控制流图：基本块划分正确，前驱/后继边完整 ★
- [ ] DAG 优化：公共子表达式消除检测到重复计算 ★
- [ ] 编译日志：7~8 个阶段完整记录，含耗时统计
- [ ] 正则→NFA：Thompson 构造法生成 NFA，显示状态和转移边 ★
- [ ] NFA→DFA：子集构造法生成 DFA，显示状态转移表 ★
- [ ] 日志扫描：DFA 正确匹配日志行，标注起止位置 ★
- [ ] 错误诊断：语法错误时显示具体错误信息
- [ ] Raw JSON：包含全部字段（tokens, ast, quads, assemblyCode, llvmIR, compileLog, cfgAnalysis 等）

---

★ 标记项为课程设计选做扩展（三.4）
