# 题目二代码说明：错误检测是如何落在代码里的

本文档聚焦 [task2_error_lexer.py](/D:/02_Development_Projects/CompilationprinciplesCourse/lab1/%E9%A2%98%E7%9B%AE%E4%BA%8C/task2_error_lexer.py) 中真正关键的实现：程序如何在扫描过程中判断“哪里错了”，以及为什么有些错误处理规则要写成现在这个样子。

## 1. 题目二和题目一的本质区别

题目一关心的是：

```text
这个片段是什么 token？
```

题目二关心的是：

```text
这个片段是否合法？如果不合法，应该报哪个错误码？
```

所以 `task2_error_lexer.py` 的重点已经不是构造 `Token`，而是维护一个错误列表：

```python
self.errors = []
```

也就是说，这个程序的输出目标从“识别成功的结果”切换成了“识别失败的结果”。

## 2. `analyze()` 为什么仍然是核心函数

`ErrorLexer.analyze()` 和题目一中的 `tokenize()` 地位是一样的，都是总调度器。

它维护的关键状态有：

- `self.index`：当前扫描位置
- `self.line`：当前行号
- `self.errors`：错误输出列表

主循环仍然是逐字符扫描：

```python
while self.index < self.length:
    ch = self.source[self.index]
```

但和题目一不同的是，这里每个分支都在回答一个问题：

```text
当前位置是合法开始，还是应该立即记一次错误？
```

## 3. 这份代码最重要的设计：扫描和报错同时进行

题目二并不是“先词法分析，再单独检查错误”，而是边扫描边判断。

这意味着：

- 一旦发现非法字符，可以立刻记 `101`
- 一旦发现构词不合法，可以立刻记 `102`
- 一旦发现字符串或字符常量没有闭合，可以立刻记 `104` 或 `105`

这样实现的好处是简单直接，而且很符合手写词法分析器的工作方式。

## 4. `_record_error()` 是个很小但很关键的函数

```python
def _record_error(self, line, code):
    if (line, code) not in self.errors:
        self.errors.append((line, code))
```

它做的不是单纯“append”，而是“去重后 append”。

这背后有一个很重要的评测适配逻辑：同一行里即使同一种错误出现多次，通常也只输出一次。

最典型的例子是：

```txt
float b_2 = 20.
```

这行里可能同时触发两个 `102`：

- `b_2` 不符合当前题目要求的标识符规则
- `20.` 小数点后没有数字

但评测往往只希望看到：

```txt
1 102
```

所以 `_record_error()` 的价值不在于“存起来”，而在于“按题目输出规则约束存储方式”。

## 5. `101` 为什么最容易实现

非法字符 `101` 的判定逻辑最直接。

当程序排除了以下所有合法可能后：

- 空白符
- 换行
- 注释
- 标识符/关键字
- 数字
- 字符常量
- 字符串
- 双字符运算符
- 单字符运算符
- 界符

剩下的字符就可以直接记成：

```python
self._record_error(self.line, ERROR_ILLEGAL_CHAR)
```

所以 `101` 本质上是一种“兜底错误”。

## 6. `102` 是整份代码里最值得重点讲的部分

`102` 表示“不符合构词规则”，它不是单一错误，而是一类错误。

在这份实现里，最关键的两类情况是：

- 非法标识符
- 非法数字形式

### 6.1 为什么下划线会触发 `102`

`_scan_identifier_or_keyword()` 里专门记录了是否出现过下划线：

```python
has_underscore = False
```

然后在扫描中：

```python
if ch == "_":
    has_underscore = True
    self.index += 1
    continue
```

最后如果 `has_underscore` 为真，就直接记：

```python
self._record_error(self.line, ERROR_BAD_TOKEN)
```

这说明这份代码不是按照一般编程语言的常见规则来处理标识符，而是按当前实验平台样例来适配：

```text
只要标识符中出现下划线，就按 102 处理。
```

这点非常值得在文档里明确写出来，否则读代码的人会误以为这里写错了。

### 6.2 为什么数字错误集中在 `_scan_number()` 里

`_scan_number()` 做的重点不是“把数字读完”，而是检查数字之后还能不能继续合法地构词。

主要有两个关键判断。

第一种：小数点后没有数字。

```python
if self.index < self.length and self.source[self.index] == ".":
    if self._peek().isdigit():
        ...
    else:
        self.index += 1
        self._record_error(start_line, ERROR_BAD_TOKEN)
        return
```

这里说明：

- `3.14` 合法
- `20.` 非法，记 `102`

第二种：数字后紧跟字母或下划线。

```python
if self.index < self.length and (
    self.source[self.index].isalpha() or self.source[self.index] == "_"
):
```

例如：

- `12abc`
- `12_a`

这些都不是合法数字，也不是合法标识符开头，所以统一记 `102`。

这也是题目二中最能体现“构词规则检查”的地方。

## 7. 多行注释错误为什么要特别讲

`_skip_multi_line_comment()` 有一个很重要的细节：如果一直扫到文件尾都没找到 `*/`，报错时使用的是当前扫描结束时的行号。

关键代码是：

```python
self._record_error(self.line, ERROR_UNCLOSED_COMMENT)
self.index = self.length
```

这意味着它记录的不是注释开始行，而是扫描终止行。

这么写不是随意选择，而是因为当前实验平台更接近这种判定方式。也就是说，这段实现不是“最通用的编译原理定义”，而是“当前题目环境下更容易过评测的定义”。

这一点非常适合写进文档，因为它体现了代码里的真实设计取舍。

## 8. 字符和字符串错误的共同模式

### 8.1 `_scan_char_literal()`

字符常量的处理逻辑是：

1. 跳过开头的 `'`
2. 读取一个字符，或者一个转义字符
3. 检查是否存在结束的 `'`
4. 如果没有，就记 `104`

重点不在于字符常量有多复杂，而在于它必须自己维护“成对引号”的规则。

### 8.2 `_scan_string_literal()`

字符串的处理逻辑则是：

1. 跳过开头的 `"`
2. 持续向后扫描
3. 遇到转义符时跳过下一个字符
4. 遇到配对 `"` 时正常结束
5. 遇到换行或文件结束仍未闭合，就记 `105`

这两个函数共同说明了一件事：

```text
引号类 token 的关键不是首字符判断，而是“如何找到合法终止位置”。
```

## 9. 为什么注释分支必须放在前面

在 `analyze()` 里，注释判断出现在标识符和运算符前面：

```python
if ch == "/" and self._peek() == "/":
    self._skip_single_line_comment()
    continue

if ch == "/" and self._peek() == "*":
    self._skip_multi_line_comment()
    continue
```

这样做的原因和题目一相同，但在题目二里更重要，因为如果注释没有优先处理：

- `/*` 可能会被拆成两个合法运算符
- 原本应该报 `103` 的情况就根本进不到注释处理逻辑里

所以这不只是“识别顺序问题”，也是“错误能不能被正确发现”的问题。

## 10. 这份代码的真正难点不在复杂，而在规则边界

题目二代码看起来不长，但真正难的地方不是语法，而是边界条件：

- 什么算非法字符
- 什么算非法标识符
- `20.` 应不应该拆开，还是直接报错
- 多行注释未闭合时到底报哪一行
- 同一行同类错误是否重复输出

这些都不是 Python 技术问题，而是“你如何把题目要求翻译成稳定的代码规则”。

所以这份文档最应该讲清楚的，恰恰就是这些实现边界。

## 11. 推荐的读代码顺序

为了最快抓住重点，建议这样读：

1. 先看 `analyze()`，理解总流程。
2. 再看 `_record_error()`，明白输出规则。
3. 然后看 `_scan_number()`，这是 `102` 的核心来源。
4. 再看 `_skip_multi_line_comment()`，理解 `103` 的行号策略。
5. 最后看字符和字符串处理，对应 `104`、`105`。

## 12. 一句话总结这份实现

这份错误词法分析器最关键的地方不是“列出 5 个错误码”，而是：

```text
它把题目中的错误定义嵌进了扫描过程本身，并通过去重、前瞻和边界条件处理，让输出符合评测预期。
```
